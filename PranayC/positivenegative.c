#include<stdio.h>
#include<conio.h>
void main()
{
    int num;
    printf("enter any number\n");
    scanf("%d",&num);
    if(num>0)
    {
        printf("the number is posiitve");
    }
    else
    {
        printf("the number is negative");
    }
    getch();
}
