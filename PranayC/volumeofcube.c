#include<stdio.h>
#include<conio.h>
void main()
{
    int l,volume;
    printf("Enter the length of cube\n");
    scanf("%d",&l);
    volume=(l*l*l);
    printf("volume of cube is %d",volume);
    getch();
}
